# Local-only docs — not shared with the team, kept out of diffs and `git add .`
CLAUDE.md
architecture-docs/

# MAVERIK config (./config/) — real files hold secrets/user-specific values; every one has a
# committed config/*.example.* template to copy from instead.
config/.env
config/agents.json
config/llm-models.json
config/mcp-servers.json

# Filled-in Docker Compose config (the .example template stays committed)
docker-compose.yml

# Per-session LLM wire logs (MCPHOST_LLM_DEBUG)
logs/

# MAVERIK benchmark output (results/{runId}/run.json + summary.csv)
results/

# Build output
[Bb]in/
[Oo]bj/
[Oo]ut/
[Ll]og/
[Ll]ogs/

# .NET / build artifacts
project.lock.json
project.fragment.lock.json
artifacts/
*.binlog

# User-specific files
*.user
*.suo
*.userosscache
*.sln.docstates
*.userprefs

# Visual Studio
.vs/
*.vsp
*.vspx
*.sap
*.svclog

# VS Code
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Rider / JetBrains
.idea/
*.sln.iml

# Test / coverage results
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.*
coverage*.json
coverage*.xml
coverage*.info
*.coverage
*.coveragexml
TestResults/

# NuGet
*.nupkg
*.snupkg
.nuget/
**/[Pp]ackages/*
!**/[Pp]ackages/build/
*.nuget.props
*.nuget.targets

# MSBuild / publish
publish/
*.[Pp]ublish.xml
*.pubxml
*.pubxml.user
PublishScripts/

# ASP.NET / EF
*.mdf
*.ldf
*.ndf
ScaffoldingReadMe.txt

# Secrets / local config (do NOT commit tokens like your GitHub PAT).
# config/.env.example stays committed — config/.env above is the only real one expected,
# this is just a safety net for a stray .env anywhere else in the tree.
appsettings.*.local.json
secrets.json
*.env

# OS files
.DS_Store
Thumbs.db
ehthumbs.db

# Misc
*.tmp
*.bak
*.cache
