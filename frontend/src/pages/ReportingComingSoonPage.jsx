// Placeholder for the Dashboards/Reports tabs — Graphs and Tables (the data layer these will
// compose) ship first; see the reporting plan for the rest of the build order.
export function ReportingComingSoonPage({ label }) {
  return <p className="muted">{label} are coming soon.</p>;
}
